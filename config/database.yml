
development:
    adapter: postgresql
    encoding: unicode
    database: fsqcats_dev
    username: <%= ENV['USER'] || 'root' %>
    password:
    host: localhost
    port: 5432

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: postgresql
    encoding: unicode
    database: fsqcats_test
    username: <%= ENV['USER'] || 'root' %>
    password:
    host: localhost
    port: 5432

production:
    adapter: postgresql
    encoding: unicode
    database: fsqcats_production
    username: <%= ENV['USER'] || 'root' %>
    password:
    host: localhost
    port: 5432
